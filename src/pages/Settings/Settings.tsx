import React, { Fragment } from 'react'

interface Props {
    
}

const Settings = (props: Props) => {
    return (
        <Fragment>
            
        </Fragment>
    )
}

export default Settings
